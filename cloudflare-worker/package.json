{
  "name": "image-studio-cloudflare-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test ./test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}
