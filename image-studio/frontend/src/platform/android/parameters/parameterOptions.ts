export const ANDROID_BATCH_COUNT_OPTIONS = [1, 2, 4, 6, 8, 9].map((count) => ({
  value: count,
  label: String(count),
}));

export const ANDROID_CONTINUOUS_CONCURRENCY_OPTIONS = [1, 2].map((count) => ({
  value: count,
  label: String(count),
}));
