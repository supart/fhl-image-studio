# FHL Studio V2.0.3 验收报告

状态：已于 2026-07-29 通过正式发布审核。GitHub 正式发布尚未执行。

## 发布依据

- 正式审核集复制自用户已验收的 V2.0.3 跨电脑测试包。
- 已测试的 Windows Portable EXE 和 ZIP 均按字节保留，未重新构建。
- 原始预发布目录和 ZIP 保持不动，作为回滚材料。
- 独立销售的 Photoshop 插件不包含在桌面端源码归档或 GitHub Release 附件中；仓库仅保留 Bridge 兼容代码、测试和文档。

## 工具链与自动化

- GitHub CI 和 Release 使用 Node.js 24.13.1、Go 1.26.3 与 Wails v2.12.0。
- CI 产品版本、前端版本、Vite 版本和 CLI 链接版本均为 2.0.3。
- 本地验证使用 Node.js 24.13.1、npm 11.8.0、Go 1.26.4 与 Wails v2.12.0。
- 发布自动化只构建 Windows Portable ZIP，不构建 Setup、MSI 或 MSIX 安装程序。

## 自动化验证

- 前端 Node 测试：581/581 通过。
- 前端 UI 测试：8 个文件中的 24/24 通过。
- TypeScript 类型检查：通过。
- ESLint：0 个错误；63 条既有允许警告。
- Windows 前端生产构建：通过，转换 1,956 个模块，版本为 2.0.3。
- 桌面端 Go 测试和 `go vet ./...`：通过。
- Go CLI 测试和 `go vet ./...`：通过。
- Cloudflare Worker 测试：5/5 通过。
- 第一次在干净源码上运行 Go 检查时，因源码交付刻意排除 `frontend/dist` 而正确停止；先完成所需 Windows 前端构建后，全部 Go 检查均通过。

## 干净状态与安全验证

- 源码合规扫描：0 个问题。
- Portable 文件夹和 ZIP 发布安全扫描：0 个问题。
- 未打包 `cli.env.local`、`fhl-api.local.json`、`browser-jobs.v1.json`、WebView 配置、API 凭据、生成输出或用户历史。
- Portable ZIP 是干净的分发产物。首次启动会在包内创建 `config/webview` 运行数据；已在本机启动过的解压目录不再是可重新分发的干净产物。
- ZIP 永不存储 API 凭据。同一 Windows 账户上，Portable 可能识别 Windows Credential Manager 中既有的 `FHL Studio` 条目，以便升级保留用户原有配置；新 Windows 账户或新电脑没有这些条目，会以未配置状态启动。
- 源码私有路径扫描未发现用户名、开发盘路径、`.prerelease-build`、5174 端口、剪贴板临时路径或 AppData 临时路径。
- 未包含 Photoshop CCX、ZXP、插件 ZIP、插件 EXE、插件源码或销售交付物。
- 未包含 Setup/MSI/MSIX 安装器或 `Codex Debug.exe`。`runtime/cli/gptcodex-image.exe` 是必需的正式命令行运行时，不是调试构建。
- 在生成最终源码归档前，已移除仅用于验证的 `node_modules` 和 `frontend/dist`。

## 正式默认状态

- 交付归档不含 API Profile 元数据或当前 API 选择。同一 Windows 账户若已有兼容的 FHL Studio 凭据，程序首次启动后可能显示系统中已有的凭据；这不表示凭据被写入交付包。
- 不含生成任务、历史条目、输入图片、输出图片或缩略图。
- 初始生图模式为文生图、单张、连续模式关闭、1:1、2K、标准质量和 PNG 输出。
- Bridge 可在没有凭据时启动，并报告当前 Profile 尚未就绪；不会向任何上游发起请求。

## 已测试运行产物完整性

- 桌面 EXE：20,770,304 字节。
- 桌面 EXE SHA-256：`0FCFC0DE8078393EF38F13C1877F523975793D6C8BEACA0756C9DC6FC37FD8E6`。
- Portable ZIP：12,131,016 字节。
- Portable ZIP SHA-256：`45B7BCB232F967531A8BE03FFD784A31754EE3E1EB6BA33120A3B29B68182FF1`。
- 两份复制的审核产物均与已验收的跨电脑测试包完全一致。

## 发布边界

- 正式整理期间未发起真实或付费的生图请求。
- 未读取、复制、删除或打包任何 API 凭据。
- 5174 端口的开发配置留在发布审核目录之外，且有意保持不变。
- Windows 专用分支 `release/windows-v2.0.3` 已推送；GitHub CI 运行 `30447276745` 全部通过。正式 Release 将使用新的 Windows 专用标签 `windows-v2.0.3-release`，不改写早期准备标签，也不影响任何 macOS 标签或 Release。
